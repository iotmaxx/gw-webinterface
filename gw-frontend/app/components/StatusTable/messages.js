/*
 * StatusTable Messages
 *
 * This contains all the text for the StatusTable component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StatusTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusTable component!',
  },
});
