/*
 * StatusOverview Messages
 *
 * This contains all the text for the StatusOverview component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StatusOverview';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusOverview component!',
  },
});
