/*
 * ContentHeader Messages
 *
 * This contains all the text for the ContentHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ContentHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ContentHeader component!',
  },
});
