/*
 * SettingsView Messages
 *
 * This contains all the text for the SettingsView component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SettingsView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SettingsView component!',
  },
});
