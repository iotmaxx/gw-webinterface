/*
 * WirelessNetworkRadioSetup Messages
 *
 * This contains all the text for the WirelessNetworkRadioSetup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WirelessNetworkRadioSetup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WirelessNetworkRadioSetup container!',
  },
});
