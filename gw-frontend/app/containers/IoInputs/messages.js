/*
 * IoInputs Messages
 *
 * This contains all the text for the IoInputs container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IoInputs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IoInputs container!',
  },
});
