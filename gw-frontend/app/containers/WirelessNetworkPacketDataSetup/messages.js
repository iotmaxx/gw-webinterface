/*
 * WirelessNetworkPacketDataSetup Messages
 *
 * This contains all the text for the WirelessNetworkPacketDataSetup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WirelessNetworkPacketDataSetup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WirelessNetworkPacketDataSetup container!',
  },
});
