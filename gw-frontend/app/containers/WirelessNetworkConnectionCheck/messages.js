/*
 * WirelessNetworkConnectionCheck Messages
 *
 * This contains all the text for the WirelessNetworkConnectionCheck container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WirelessNetworkConnectionCheck';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WirelessNetworkConnectionCheck container!',
  },
});
