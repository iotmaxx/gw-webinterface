/*
 * SystemHardware Messages
 *
 * This contains all the text for the SystemHardware container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SystemHardware';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SystemHardware container!',
  },
});
