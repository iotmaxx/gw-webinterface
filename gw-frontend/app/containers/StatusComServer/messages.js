/*
 * StatusComServer Messages
 *
 * This contains all the text for the StatusComServer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StatusComServer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusComServer container!',
  },
});
