/*
 * SystemUpAndDownload Messages
 *
 * This contains all the text for the SystemUpAndDownload container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SystemUpAndDownload';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SystemUpAndDownload container!',
  },
});
