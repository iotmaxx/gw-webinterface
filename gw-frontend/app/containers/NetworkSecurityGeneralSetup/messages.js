/*
 * NetworkSecurityGeneralSetup Messages
 *
 * This contains all the text for the NetworkSecurityGeneralSetup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NetworkSecurityGeneralSetup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NetworkSecurityGeneralSetup container!',
  },
});
