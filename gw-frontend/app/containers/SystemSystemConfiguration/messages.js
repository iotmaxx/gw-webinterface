/*
 * SystemSystemConfiguration Messages
 *
 * This contains all the text for the SystemSystemConfiguration container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SystemSystemConfiguration';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SystemSystemConfiguration container!',
  },
});
