/*
 * StatusNetworkConnections Messages
 *
 * This contains all the text for the StatusNetworkConnections container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StatusNetworkConnections';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusNetworkConnections container!',
  },
});
