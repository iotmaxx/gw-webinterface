/*
 * IoOutputs Messages
 *
 * This contains all the text for the IoOutputs container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IoOutputs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IoOutputs container!',
  },
});
