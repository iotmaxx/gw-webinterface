/*
 * SystemConfiguration Messages
 *
 * This contains all the text for the SystemConfiguration container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SystemConfiguration';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SystemConfiguration container!',
  },
});
