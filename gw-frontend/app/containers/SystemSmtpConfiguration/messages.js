/*
 * SystemSmtpConfiguration Messages
 *
 * This contains all the text for the SystemSmtpConfiguration container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SystemSmtpConfiguration';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SystemSmtpConfiguration container!',
  },
});
