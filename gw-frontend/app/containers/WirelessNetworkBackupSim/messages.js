/*
 * WirelessNetworkBackupSim Messages
 *
 * This contains all the text for the WirelessNetworkBackupSim container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WirelessNetworkBackupSim';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WirelessNetworkBackupSim container!',
  },
});
