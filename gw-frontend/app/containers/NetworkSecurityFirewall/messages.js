/*
 * NetworkSecurityFirewall Messages
 *
 * This contains all the text for the NetworkSecurityFirewall container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NetworkSecurityFirewall';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NetworkSecurityFirewall container!',
  },
});
