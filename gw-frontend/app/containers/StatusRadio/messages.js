/*
 * StatusRadio Messages
 *
 * This contains all the text for the StatusRadio container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StatusRadio';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusRadio container!',
  },
});
