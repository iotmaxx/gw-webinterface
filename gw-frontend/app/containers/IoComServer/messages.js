/*
 * IoComServer Messages
 *
 * This contains all the text for the IoComServer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IoComServer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IoComServer container!',
  },
});
