/*
 * LocalNetworkDhcpServer Messages
 *
 * This contains all the text for the LocalNetworkDhcpServer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LocalNetworkDhcpServer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LocalNetworkDhcpServer container!',
  },
});
