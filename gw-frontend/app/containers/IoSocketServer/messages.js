/*
 * IoSocketServer Messages
 *
 * This contains all the text for the IoSocketServer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IoSocketServer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IoSocketServer container!',
  },
});
