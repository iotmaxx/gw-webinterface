/*
 * SystemSnmpConfiguration Messages
 *
 * This contains all the text for the SystemSnmpConfiguration container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SystemSnmpConfiguration';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SystemSnmpConfiguration container!',
  },
});
