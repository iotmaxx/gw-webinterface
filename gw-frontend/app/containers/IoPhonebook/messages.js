/*
 * IoPhonebook Messages
 *
 * This contains all the text for the IoPhonebook container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IoPhonebook';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IoPhonebook container!',
  },
});
