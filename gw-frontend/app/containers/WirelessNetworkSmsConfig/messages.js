/*
 * WirelessNetworkSmsConfig Messages
 *
 * This contains all the text for the WirelessNetworkSmsConfig container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WirelessNetworkSmsConfig';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WirelessNetworkSmsConfig container!',
  },
});
