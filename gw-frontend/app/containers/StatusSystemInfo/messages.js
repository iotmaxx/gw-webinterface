/*
 * StatusSystemInfo Messages
 *
 * This contains all the text for the StatusSystemInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StatusSystemInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusSystemInfo container!',
  },
});
