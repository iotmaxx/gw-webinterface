/*
 * WirelessNetworkSim Messages
 *
 * This contains all the text for the WirelessNetworkSim container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WirelessNetworkSim';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WirelessNetworkSim container!',
  },
});
