/*
 * StatusIoStats Messages
 *
 * This contains all the text for the StatusIoStats container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.StatusIoStats';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StatusIoStats container!',
  },
});
